<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Hero Section - Admin</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="admin-page">
    <div class="admin-container">
        <%- include('../partials/admin-sidebar') %>
        
        <div class="admin-content">
            <h2 class="section-title">Edit Hero Section</h2>

            <div class="admin-card">
                <form action="/admin/hero" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label>Greeting Text</label>
                        <input type="text" name="greeting" value="<%= hero.greeting %>" required>
                    </div>
                    <div class="form-group">
                        <label>Main Title</label>
                        <input type="text" name="title" value="<%= hero.title %>" required>
                    </div>
                    <div class="form-group">
                        <label>Subtitle / Bio</label>
                        <textarea name="subtitle" rows="4" required><%= hero.subtitle %></textarea>
                    </div>
                    <div class="form-group">
                        <label>Hero Image</label>
                        <% if(hero.heroImage) { %>
                            <div style="margin-bottom: 10px;">
                                <img src="<%= hero.heroImage %>" alt="Hero Image" style="max-width: 200px; border-radius: 8px;">
                            </div>
                        <% } %>
                        <input type="file" name="heroImage">
                        <small>Upload an image to display in the 3D card</small>
                    </div>
                    <div class="form-group">
                        <label>Primary CTA Text (e.g., View My Work)</label>
                        <input type="text" name="primaryCtaText" value="<%= hero.primaryCtaText %>" required>
                    </div>
                    <div class="form-group">
                        <label>Secondary CTA Text (e.g., Contact Me)</label>
                        <input type="text" name="secondaryCtaText" value="<%= hero.secondaryCtaText %>" required>
                    </div>
                    <button type="submit" class="btn">Save Changes</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
