<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit About - Admin</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="admin-page">
    <div class="admin-container">
        <%- include('../partials/admin-sidebar') %>
        
        <div class="admin-content">
            <h2 class="section-title">Edit About Info</h2>
            
            <div class="admin-card">
                <form action="/admin/about" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label>Section Title (e.g., About Me)</label>
                        <input type="text" name="title" value="<%= about ? about.title : 'About Me' %>" required>
                    </div>
                    <div class="form-group">
                        <label>Name</label>
                        <input type="text" name="name" value="<%= about ? about.name : '' %>" required>
                    </div>
                    <div class="form-group">
                        <label>Role</label>
                        <input type="text" name="role" value="<%= about ? about.role : '' %>" required>
                    </div>
                    <div class="form-group">
                        <label>Profile Image</label>
                        <% if(about && about.profileImage) { %>
                            <div style="margin-bottom: 10px;">
                                <img src="<%= about.profileImage %>" alt="Profile" style="max-width: 150px; border-radius: 50%;">
                            </div>
                        <% } %>
                        <input type="file" name="profileImage">
                    </div>
                    <div class="form-group">
                        <label>Resume (PDF/Image)</label>
                        <% if(about && about.resumeUrl) { %>
                            <div style="margin-bottom: 10px;">
                                <a href="<%= about.resumeUrl %>" target="_blank">View Current Resume</a>
                            </div>
                        <% } %>
                        <input type="file" name="resume">
                    </div>
                    <div class="form-group">
                        <label>Bio</label>
                        <textarea name="bio" rows="6" required><%= about ? about.bio : '' %></textarea>
                    </div>
                    <button type="submit" class="btn">Save Changes</button>
                    <a href="/admin/dashboard" class="btn btn-danger" style="margin-left: 10px;">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
